% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simon_trial.R
\name{simon_design}
\alias{simon_design}
\title{Create Simon two-stage design specification}
\usage{
simon_design(
  n_baskets,
  basket_names = NULL,
  sample_sizes,
  null_response_rates,
  alternative_response_rates,
  alpha = 0.05,
  beta = 0.2,
  design_type = c("optimal", "minimax")
)
}
\arguments{
\item{n_baskets}{Integer, number of independent cohorts/baskets}

\item{basket_names}{Optional character vector of cohort names}

\item{sample_sizes}{Integer vector of total sample sizes per cohort.
If length 1, same size used for all cohorts.}

\item{null_response_rates}{Numeric vector of null response rates (p0).
If length 1, same p0 used for all cohorts.}

\item{alternative_response_rates}{Numeric vector of alternative response rates (p1).
If length 1, same p1 used for all cohorts. Must be > null_response_rates.}

\item{alpha}{Significance level per cohort (default 0.05)}

\item{beta}{Type II error rate per cohort (default 0.20)}

\item{design_type}{Character, "optimal" (default, minimizes EN) or
"minimax" (minimizes maximum n)}
}
\value{
An object of class \code{simon_design} containing:
\itemize{
\item \code{n_baskets}: Number of independent cohorts
\item \code{basket_names}: Names of cohorts
\item \code{sample_sizes}: Total sample sizes
\item \code{null_response_rates}: Null response rates
\item \code{alpha}: Significance level
\item \code{beta}: Type II error rate
\item \code{design_type}: Design optimization criterion
}
}
\description{
Creates a design object for parallel independent Simon two-stage designs.
This is not a basket design (no information borrowing), but is included
as a reference comparator to evaluate information borrowing methods.
}
\details{
Simon two-stage designs (Simon 1989) are the standard for single-arm phase II
trials. For basket trials, running parallel Simon designs provides a reference
that controls per-cohort error rates without borrowing information.

Each cohort operates independently:
\itemize{
\item Stage 1: Enroll n1 patients. Stop for futility if <= r1 responses.
\item Stage 2: Enroll n2 = n - n1 additional patients.
\item Decision: Reject H0 if total responses > r.
}

Use Bonferroni correction for family-wise error control:
Set alpha = FWER / n_baskets.
}
\examples{
# Design for 4 independent cohorts
design <- simon_design(
  n_baskets = 4,
  basket_names = paste0("Cohort", 1:4),
  sample_sizes = 25,
  null_response_rates = 0.20,
  alternative_response_rates = 0.40,
  alpha = 0.05 / 4,  # Bonferroni for FWER control
  beta = 0.20
)

print(design)
}
\references{
Simon R (1989). Optimal two-stage designs for phase II clinical trials.
\emph{Controlled Clinical Trials}, 10(1):1-10.
}
