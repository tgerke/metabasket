% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basket_trial.R
\name{basket_design}
\alias{basket_design}
\title{Create a basket trial design specification}
\usage{
basket_design(
  n_baskets,
  basket_names = NULL,
  sample_sizes,
  response_rates = NULL,
  null_response_rates,
  design_type = c("bma", "mem", "bhm", "cunanan"),
  design_params = list()
)
}
\arguments{
\item{n_baskets}{Integer. Number of baskets (cancer types/subtypes) in the trial}

\item{basket_names}{Character vector. Names of the baskets}

\item{sample_sizes}{Integer vector. Sample size per basket (can be single value or vector)}

\item{response_rates}{Numeric vector. True response rates under alternative (for simulation)}

\item{null_response_rates}{Numeric vector. Null response rates to test against}

\item{design_type}{Character. Type of design: "bma" (Bayesian model averaging),
"mem" (multi-source exchangeability), "bhm" (Bayesian hierarchical model),
or "cunanan" (Cunanan et al. 2017)}

\item{design_params}{List. Additional design-specific parameters}
}
\value{
An object of class 'basket_design' containing the trial specification
}
\description{
Creates a structured specification for a basket trial design that can be used
across different analysis methods (BMA, hierarchical models, etc.)
}
\examples{
# Create a simple 4-basket design
design <- basket_design(
  n_baskets = 4,
  basket_names = c("NSCLC", "SCLC", "Melanoma", "RCC"),
  sample_sizes = 25,
  response_rates = c(0.35, 0.35, 0.35, 0.35),
  null_response_rates = 0.20,
  design_type = "bma"
)
}
